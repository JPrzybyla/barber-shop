import React from 'react';
import {withRouter} from 'react-router-dom';

//import css
import './admin.scss';

function Admin(){

    return (
        <div>
            dupaa
        </div>
    );
}

export default withRouter(Admin);
